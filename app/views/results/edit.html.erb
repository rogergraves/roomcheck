<h1>Edit Problem: <%= @result.check_item.name %></h1>

<%= render :partial => 'layouts/result' %>

<%= link_to "Problem Fixed", result_path(:result_id => @result.id), :method => 'DELETE', :confirm => 'Are you sure?'%>
<br />

<%= link_to "Go back", check_list_path(@result.check_item.check_list.id) %>